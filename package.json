{
  "name": "geese-poop-spotter",
  "version": "1.0.0",
  "description": "Geese Poop Spotter app for University of Waterloo campus",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "build:server": "cd server && npm install --production=false",
    "build:client": "cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "start:prod": "npm run build && PORT=5001 NODE_ENV=production npm start",
    "postinstall": "npm run install-all"
  },
  "keywords": ["geese", "waterloo", "campus", "map"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
